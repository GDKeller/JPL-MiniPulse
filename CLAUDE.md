# JPL-MiniPulse

## Build

- **PlatformIO CLI (macOS)**: `~/.platformio/penv/bin/pio`
- Build all targets: `~/.platformio/penv/bin/pio run`
- Build a specific env: `~/.platformio/penv/bin/pio run -e release`
- Available environments: `release`, `debug`, `devdebug`
